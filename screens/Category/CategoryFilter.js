import {View, Text} from 'react-native';
import React from 'react';

const CategoryFilter = ({navigation, route}) => {
  return (
    <View>
      <Text className="text-blue-600">CategoryFilter</Text>
    </View>
  );
};

export default CategoryFilter;
